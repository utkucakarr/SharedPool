version: '3.8'

services:
  postgres-db: 
    image: postgres:17 
    environment:
      POSTGRES_USER: sharedpool
      POSTGRES_PASSWORD: sharedpool123
      POSTGRES_DB: SharedPoolDb
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: